export interface CertificateMakerProps {
  name: string;
  mode: string;
  reason: string;
  date: Date;
  sex: string;
  canDownload: boolean;
  template: string;
  role1: string;
  role2: string;
}
